---
'@wyreup/core': patch
---

Truth-in-metadata pass from the phase-1 tool review: descriptions now disclose actual behavior (csv-json array requirement, csv-merge dropped key column, color-palette transparency handling, word-counter markdown counting, pdf-flatten no-op passthrough, image-diff JSON stats output, pdf-metadata read-mode JSON, url-build spec-param input, strip-video-metadata container-only scope); bg-remove and color-blind-simulator declare their real image/* output; roman-numeral no longer claims file input it ignores.
