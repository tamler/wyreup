---
'@wyreup/core': patch
---

Rename the hosted OCR tool's display name to "High-Accuracy OCR" — it shared
the literal name "OCR" with the free on-device tool, making the pair
indistinguishable on tool pages, suggestions, and agent tool lists. Ids and
slugs are unchanged.
