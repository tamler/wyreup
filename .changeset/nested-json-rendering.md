---
'@wyreup/web': patch
---

Readable rendering for nested JSON results: arrays of uniform objects render as tables, sectioned objects render as labeled groups with stat grids and joined lists, all with size caps and the raw-JSON fallback/toggle intact. Covers the 17 tools from the phase-1 review whose human-facing output was still a raw code block.
