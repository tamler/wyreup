{
  "name": "@wyreup/worker-models",
  "version": "0.0.0",
  "description": "Cloudflare Worker proxying models.wyreup.com — serves AI model assets from R2 with lazy upstream mirror.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "populate-manifest": "tsx scripts/populate-manifest.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260710.1",
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.2.7",
    "wrangler": "^4.84.0"
  }
}
