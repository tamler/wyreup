---
import BaseLayout from '../layouts/BaseLayout.astro';
import ToolbeltComponent from '../components/Toolbelt.svelte';
---

<BaseLayout
  title="Toolbelt — Wyreup"
  description="Your toolbelt — saved chains plus trigger rules. Lives on this device. Drop a file, and a matching rule proposes its chain (never silently)."
>
  <section class="chains-page">
    <div class="container">
      <nav class="breadcrumb" aria-label="Breadcrumb">
        <a href="/" class="breadcrumb__link">Wyreup</a>
        <span class="breadcrumb__sep" aria-hidden="true">/</span>
        <span class="breadcrumb__current" aria-current="page">Toolbelt</span>
      </nav>

      <header class="page-header">
        <h1 class="page-title">Toolbelt</h1>
        <p class="page-subhead">Your saved chains and trigger rules. Lives on this device only. Export as JSON to move between devices.</p>
        <div class="page-actions">
          <a href="/chain/build" class="btn-primary">Build a chain</a>
        </div>
      </header>

      <ToolbeltComponent client:load />
    </div>
  </section>
</BaseLayout>

<style>
  .chains-page {
    padding: var(--space-8) 0 var(--space-16);
  }

  .container {
    max-width: 860px;
  }

  .breadcrumb {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    margin-bottom: var(--space-6);
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    color: var(--text-subtle);
  }

  .breadcrumb__link {
    color: var(--text-muted);
    text-decoration: none;
    transition: color var(--duration-instant) var(--ease-sharp);
  }

  .breadcrumb__link:hover { color: var(--text-primary); }
  .breadcrumb__sep { color: var(--text-subtle); }
  .breadcrumb__current { color: var(--text-primary); }

  .page-header {
    margin-bottom: var(--space-8);
  }

  .page-title {
    font-family: var(--font-sans);
    font-size: var(--text-2xl);
    font-weight: 700;
    letter-spacing: -0.03em;
    color: var(--text-primary);
    line-height: 1.1;
    margin-bottom: var(--space-3);
  }

  .page-subhead {
    font-family: var(--font-sans);
    font-size: var(--text-base);
    color: var(--text-muted);
    line-height: 1.6;
    max-width: 600px;
    margin-bottom: var(--space-6);
  }

  .page-actions {
    display: flex;
    gap: var(--space-3);
  }

  .btn-primary {
    height: 32px;
    padding: 0 var(--space-3);
    background: var(--accent);
    color: var(--black);
    border: none;
    border-radius: var(--radius-md);
    font-family: var(--font-mono);
    font-size: var(--text-base);
    font-weight: 500;
    cursor: pointer;
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    transition: background var(--duration-instant) var(--ease-sharp);
  }

  .btn-primary:hover { background: var(--accent-hover); }
  .btn-primary:focus-visible { outline: 2px solid var(--accent-hover); outline-offset: 2px; }
</style>
